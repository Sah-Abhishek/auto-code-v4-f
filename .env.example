# Backend API base URL.
# NOTE: Vite inlines any VITE_* env var into the public client bundle,
# so do NOT put real secrets here. URLs and feature flags only.
VITE_BACKEND_URL=http://localhost:8200/api
